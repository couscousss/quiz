# Dependencies (installed by the host at build time)
node_modules/
package-lock.json

# Root-level npm artifacts (the runtime app's manifest lives in web/package.json)
/package.json

# clasp (Apps Script local workflow)
.clasp.json
.clasprc.json

# OS / editor cruft
.DS_Store
*.log
